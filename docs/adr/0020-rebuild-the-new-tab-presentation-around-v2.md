# Rebuild the New Tab presentation around V2

The approved V2 prototype represents a new presentation architecture, not a theme to layer onto the V1 component tree. The migration will rebuild the New Tab layout, visual system, and interaction surfaces around `design/v2/index.html` while reusing the existing message contracts, storage, synchronization, and feature services so the redesign does not become an unrelated backend rewrite.
