# Retire New Tab visual personalization in V2

The V2 New Tab uses one fixed light art direction and one fixed dark art direction so its selected visual identity remains consistent. The migration removes the Paper, Sage, Mist, and Blush palettes, custom backgrounds, and surface transparency controls, deletes their stored values and cached image data, and provides no compatibility path; persisted light/dark mode remains supported.
